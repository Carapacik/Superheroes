name: superheroes
description: api with dio
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.3
  google_fonts: ^2.1.0
  rxdart: ^0.27.1
  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  network_image_mock: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - google_fonts/
    - assets/
    - assets/images/